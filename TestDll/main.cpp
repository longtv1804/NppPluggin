#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <functional>
#include <string>
#include <list>
#include <vector>
using namespace std;
#include <regex>


int main() {
	cout << "start testing" << endl;
/*
	char time[] = "20221011T011456-0800";
	struct tm timeStamp;


	int year = 0, month = 0, day = 0, hour = 0, min = 0, sec = 0;

	if (sscanf(time, "%4d%2d%2dT%2d%2d%2d", &year, &month, &day, &hour, &min, &sec)) {
		timeStamp.tm_year = year;
		timeStamp.tm_mon = month;
		timeStamp.tm_mday = day;
		timeStamp.tm_hour = hour;
		timeStamp.tm_min = min;
		timeStamp.tm_sec = sec;
	}
*/

	return 0;
}